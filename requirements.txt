# =====================================================================
# Stack RAMPIANG untuk Python 3.14 (Streamlit Cloud pakai Python 3.14.7).
# PENTING: numpy / torch / streamlit / google-genai TIDAK di-pin, agar pip
# otomatis memilih versi yang punya PREBUILT WHEEL untuk Python 3.14.
# Versi lama (numpy 1.26.4, torch 2.4.0, streamlit 1.39.0) TIDAK punya wheel
# Py3.14 -> pip build from source -> gagal/timeout.
# Yang di-pin:
#   - sentence-transformers==3.1.1 -> menarik transformers 4.x (kecil, tanpa
#     ratusan modul vision, sehingga install cepat & log bersih).
#   - pyarrow==24.0.0 -> cegah Streamlit Cloud bolak-balik downgrade 24<->25.
# =====================================================================
streamlit
sentence-transformers==3.1.1
numpy
google-genai
torch
pyarrow==24.0.0
